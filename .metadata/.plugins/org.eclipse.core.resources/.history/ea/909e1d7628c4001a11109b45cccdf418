package toy_project;

import java.util.*;

public class tetris_block {
	public List<Point> make_block(int j) {
		List<Point> list = new ArrayList<>();

		// ㅡ 모양 블록
		if (j == 0) {
			int[] idx = { 4, 5, 6, 7 };
			for (int i = 0; i < 4; i++) {
				list.add(new Point(idx[i], 1));
			}
		} else if (j == 1) {

		} else if (j == 2) {

		} else if (j == 3) {

		} else {

		}

		return list;
	}

	private class Point {
		int x, y;

		public Point(int x, int y) {
			this.x = x;
			this.y = y;
		}
	}
}
